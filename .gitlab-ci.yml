image: java:8-jdk

stages:
  - build
  
before_script:
  - chmod +x gradlew
  
cache:
  paths:
    - .gradle/wrapper
    - .gradle/caches

build:
  stage: build
  script:
    - ./gradlew build
  artifacts:
    paths:
      - build/libs/
    expire_in: 6 months